// @obsidian/relayer — placeholder.
//
// Phase 2 will implement a service that accepts a withdrawal proof + params,
// submits the on-chain withdraw tx, and takes a fee — so the recipient never
// needs gas (preserving the privacy link). See ROADMAP.md (Phase 2).

export const PLACEHOLDER = true;
